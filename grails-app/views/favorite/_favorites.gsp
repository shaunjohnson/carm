<div id="add${type}ToFavorites" class="header-action add-to-favorites"
     style="${isFavorite ? 'display: none;' : 'display: block;'}"
     title="${message(code: "addToFavorites.message")}">&nbsp;</div>

<div id="remove${type}FromFavorites" class="header-action remove-from-favorites"
     style="${isFavorite ? 'display: block;' : 'display: none;'}"
     title="${message(code: "removeFromFavorites.message")}">&nbsp;</div>
